# docker aliases

alias dkrmdi='docker rmi $(sudo docker images -qf dangling=true)'
alias dklsi='docker images'
alias dklsc='docker ps -a'
alias dkrmc='docker rm'
alias dkrmcs='docker rm $(dklsc -q)'
alias dkrc='docker run --rm'
